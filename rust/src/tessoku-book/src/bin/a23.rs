use proconio::input;

fn main() {
    input! {
        N: u32,
        M: usize,
        AMN: [[usize; M]; N]
    }
    let mut dp = vec![vec![0; 2_usize.pow(N)]; M];
    for n in 0..N {
        for g in 0..(2_usize.pow(N)) {}
    }
}
