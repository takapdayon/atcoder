def c209(n, clist):

    mod = 10**9 + 7
    # index対応でAよりCのほうが大きい
    # Aの中に同じ数は合ってはいけない

    # 最初に総合の数を出す
    # そのあと除外パターンを引く
    

    return ans

n = int(input())
clist = list(map(int, input().split()))
print(c209(n, clist))
